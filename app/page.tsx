export default function Home() {
  return (
    <div>
      <p>Use <a href="/run">Run</a> to execute a date-window cleanup, then <a href="/review">Review</a> to approve/reject drafts.</p>
    </div>
  );
}
